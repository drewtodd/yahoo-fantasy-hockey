services:
  hockey:
    build: .
    container_name: hockey-cli
    user: "${UID}:${GID:-1000}"
    environment:
      - TZ=America/Los_Angeles
    env_file:
      - .env
    volumes:
      - ./.cache:/app/.cache
      - ./data:/app/data
    restart: "no"
